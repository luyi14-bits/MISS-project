<Window x:Class="MISS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MISS.Controls"
        xmlns:views="clr-namespace:MISS.Views"
        Title="MISS" Height="750" Width="1100"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <views:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <views:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <Style x:Key="PanelToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
            <Setter Property="Content" Value="«"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="Content" Value="»"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Role Sidebar -->
        <views:RoleSidebar x:Name="RoleSidebarView" Grid.Column="0"/>

        <!-- Center: Conversation -->
        <views:ConversationView x:Name="ConversationViewCtl" Grid.Column="1"/>

        <!-- Right: Panel toggle + collapsible AttributePanel -->
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ToggleButton Grid.Column="0" x:Name="PanelToggleBtn"
                          IsChecked="{Binding IsPanelCollapsed, Converter={StaticResource InverseBooleanConverter}}"
                          Style="{StaticResource PanelToggleButtonStyle}"/>

            <Border Grid.Column="1"
                    Visibility="{Binding IsPanelCollapsed, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <views:AttributePanel x:Name="AttributePanelView"/>
            </Border>
        </Grid>
    </Grid>
</Window>
